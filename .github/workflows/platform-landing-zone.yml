name: 'Platform Landing Zone'

on:
  push:
    branches: [ "main" ]
    paths: [ "platform/**" ]
  workflow_dispatch:


permissions:
  id-token: write
  contents: read

env:
  ARM_CLIENT_ID:       ${{ secrets.AZURE_CLIENT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  ARM_TENANT_ID:       ${{ secrets.AZURE_TENANT_ID }}
  ARM_USE_OIDC:        true
  ARM_USE_AZUREAD:     true

defaults:
  run:
    working-directory: platform

jobs:
  plan:
    name: 'Terraform Plan'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 'Az CLI login'
        uses: azure/login@v2
        with:
          client-id:       ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id:       ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="tenant_id=${{ secrets.AZURE_TENANT_ID }}" \
            -backend-config="storage_account_name=${{ secrets.STORAGE_ACCOUNT_STATE }}" \
            -backend-config="container_name=${{ secrets.CONTAINER_STATE }}" \
            -backend-config="key=platform-landing-zone.tfstate" \
            -backend-config="subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID }}" \
            -backend-config="use_azuread_auth=true" \
            -backend-config="use_oidc=true"

      - name: Terraform Plan
        run: terraform plan -input=false -var-file="management.tfvars" -out=tfplan

      - name: Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: alz-tfplan
          path: platform/tfplan
          retention-days: 1

  apply:
    name: 'Apply'
    needs: plan
    # Gated twice: a repo variable switch AND the production environment approval.
    # Apply stays OFF until you create repo variable ENABLE_APPLY=true.
    if: github.ref == 'refs/heads/main' && github.event_name == 'push' && vars.ENABLE_APPLY == 'true'
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 'Az CLI login'
        uses: azure/login@v2
        with:
          client-id:       ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id:       ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: alz-tfplan
          path: platform

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="tenant_id=${{ secrets.AZURE_TENANT_ID }}" \
            -backend-config="storage_account_name=${{ secrets.STORAGE_ACCOUNT_STATE }}" \
            -backend-config="container_name=${{ secrets.CONTAINER_STATE }}" \
            -backend-config="key=platform-landing-zone.tfstate" \
            -backend-config="subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID }}" \
            -backend-config="use_azuread_auth=true" \
            -backend-config="use_oidc=true"

      - name: Terraform Apply
        run: terraform apply -input=false -auto-approve tfplan
